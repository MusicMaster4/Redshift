<resources>
    <style name="Theme.Redshift" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:statusBarColor">@color/redshift_background</item>
        <item name="android:navigationBarColor">@color/redshift_background</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="colorAccent">@color/redshift_accent</item>
        <item name="android:windowBackground">@color/redshift_background</item>
    </style>
</resources>
